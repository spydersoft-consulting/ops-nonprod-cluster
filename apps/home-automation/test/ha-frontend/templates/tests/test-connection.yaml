---
# Source: ha-frontend/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "ha-frontend-test-connection"
  labels:
    helm.sh/chart: ha-frontend-0.1.0
    app.kubernetes.io/name: ha-frontend
    app.kubernetes.io/instance: ha-frontend
    app.kubernetes.io/version: "1.0.9"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['ha-frontend:80']
  restartPolicy: Never
